import socks
import logging



def bind_tcp(ip, port, proxy):
    """Create a TCP socket with binding remote agent"""


    logger = logging.getLogger('main')


    logger.debug(f"Remote agent IP : {ip}")
    logger.debug(f"Remote agent port : {port}")
    logger.debug(f"Proxy : {proxy}")







    return

